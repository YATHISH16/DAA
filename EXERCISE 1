SELECTION SORT:

import java.util.Scanner;

public class SelectionSortExample1 {
    public static void selectionSort(int[] arr) {
        int n = arr.length;

        for (int i = 0; i < n - 1; i++) {
            int min_idx = i;
            for (int j = i + 1; j < n; j++) {
                if (arr[j] < arr[min_idx]) {
                    min_idx = j;
                }
            }
            int temp = arr[min_idx];
            arr[min_idx] = arr[i];
            arr[i] = temp;
            System.out.print("After Pass " + (i + 1) + ": ");
            for (int k = 0; k < n; k++) {
                System.out.print(arr[k] + " ");
            }
            System.out.println();
        }
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Enter number of elements: ");
        int size = sc.nextInt();
        int[] data = new int[size];

        System.out.println("Enter " + size + " integers:");
        for (int i = 0; i < size; i++) {
            data[i] = sc.nextInt();
        }

        System.out.print("\nInitial Array: ");
        for (int num : data) {
            System.out.print(num + " ");
        }
        System.out.println("\n----------------------------");

        selectionSort(data);

        System.out.println("----------------------------");
        System.out.print("Final Sorted Array: ");
        for (int num : data) {
            System.out.print(num + " ");
        }

        sc.close();
    }
}

OUTPUT:

PS D:\java> java SelectionSort.java
Enter number of elements: 5
Enter 5 integers:
2
3
7
5
1

Initial Array: 2 3 7 5 1
----------------------------
After Pass 1: 1 3 7 5 2
After Pass 2: 1 2 7 5 3
After Pass 3: 1 2 3 5 7
After Pass 4: 1 2 3 5 7
----------------------------
Final Sorted Array: 1 2 3 5 7


BUBBLE SORT:

import java.util.Scanner;

public class BubbleSort {
    public static void bubbleSort(int[] arr) {
        int n = arr.length;
        for (int i = 0; i < n - 1; i++) {
            for (int j = 0; j < n - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }


            System.out.print("After Pass " + (i + 1) + ": ");
            for (int k = 0; k < n; k++) {
                System.out.print(arr[k] + " ");
            }
            System.out.println();
        }
    }

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        System.out.print("Enter number of elements: ");
        int size = sc.nextInt();
        int[] data = new int[size];

        System.out.println("Enter " + size + " integers:");
        for (int i = 0; i < size; i++) {
            data[i] = sc.nextInt();
        }

        System.out.println("\nInitial Array:");
        for (int num : data) {
            System.out.print(num + " ");
        }
        System.out.println("\n----------------------------");

        bubbleSort(data);

        System.out.println("----------------------------");
        System.out.println("Final Sorted Array:");
        for (int num : data) {
            System.out.print(num + " ");
        }

        sc.close();
    }
}


OUTPUT:

PS D:\java> java BubbleSort.java
Enter number of elements: 5
Enter 5 integers:
7
5
8
1
9

Initial Array:
7 5 8 1 9
----------------------------
After Pass 1: 5 7 1 8 9
After Pass 2: 5 1 7 8 9
After Pass 3: 1 5 7 8 9
After Pass 4: 1 5 7 8 9
----------------------------
Final Sorted Array:
1 5 7 8 9
